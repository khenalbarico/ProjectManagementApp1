namespace ProjectManagement1.Models.Enums;

public enum DeveloperWorkloadStatus
{
    Standby,
    UnderLoaded,
    Balanced,
    Overloaded
}

public enum DeveloperProjectExposureStatus
{
    UnderExposed,
    Optimal,
    OverExposed
}

public enum DeveloperHealthStatus
{
    Unhealthy,
    Healthy
}